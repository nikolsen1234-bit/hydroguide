# HydroGuide Agent Worklog

This file is a tracked placeholder. The real worklog is kept locally in
`.ai/agent-worklog.local.md` and never pushed to GitHub.

The pre-commit hook (`.githooks/pre-commit`) requires this file to be staged
when project files are committed, so the local agent-tracking workflow keeps
working. Contents intentionally minimal.
